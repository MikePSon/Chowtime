<%= form_for(@create_recipe_category) do |f| %>
  <% if @create_recipe_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@create_recipe_category.errors.count, "error") %> prohibited this create_recipe_category from being saved:</h2>

      <ul>
      <% @create_recipe_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
